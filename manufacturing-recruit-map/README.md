# manufacturing-recruit-map

静态站点目录，面向 Vercel 部署。

## 目录

- `index.html`: 页面入口
- `styles.css`: 样式
- `app.js`: 交互逻辑
- `jobs-data.js`: 岗位数据

## 部署

在 Vercel 导入仓库后，把 `Root Directory` 设为：

```txt
manufacturing-recruit-map
```

Framework Preset 选 `Other` 即可，构建命令留空。
